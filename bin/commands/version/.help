Displays Zowe version.

When the `--config` parameter is used, this command displays the version of Zowe found at the path specified by the `zowe.runtimeDirectory` configuration key.

Otherwise, the command displays the version of Zowe associated with the location of the `zwe` command.

# MacOS
.DS_Store

# stores ssh key
.env
# stores temporary files when running playbook
.tmp
# ansible playbook retry file
*.retry
# python compiled code
*.pyc

# test playbooks
test*

# ignore some hosts
host_vars/vm30101.yml
